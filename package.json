{
  "name": "bts",
  "private": true,
  "devDependencies": {
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ng-annotate": "^0.10.2",
    "uglify-js": "^2.4.15"
  }
}
